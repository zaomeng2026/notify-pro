# AGENTS.md

## 全局执行规范

1. 涉及 UI/UX 设计任务时，优先使用 `$ui-ux-pro-max` 技能。
2. 默认使用项目内的 `6a工作流.md` 作为执行流程基线（6A 工作流）。
3. 产出文件与文档统一使用 `UTF-8（无 BOM）` 编码。
4. 说明、注释、交付文字默认使用中文。

## 适用范围

- 本规范适用于本仓库中的设计、开发、评审与交付环节。
- 如用户在当前会话中提出更高优先级的新要求，以最新用户要求为准。
